# Development / test dependencies. Install with:
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0,<9.0
ruff>=0.4,<1.0      # lint + import sorting (configured in pyproject.toml)
